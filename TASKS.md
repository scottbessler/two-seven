1. revamp tables. instead of creating tables we should just have the different permutations available. players click what type to join. if it seems like too many permutations we can cut some, aim for 10-15 options. cash games should fill the table with random bots (current engines but create 3 unique named bots for each engine). when a human sits, they kick out the lowest stacked bot. once a table fills with humans, create a 2nd table. player move timeout should be added, we'll want to tweak this but lets start at 8s. also if they timeout 3 times in a row set them to "auto-skip" until they hit a "im back" button. 

2. blackjack and hand blitz both need touchups for mobile screens. they both should not need scrolling. also buttons and important locations to look at (like your cards) should not be moving around as things render.
